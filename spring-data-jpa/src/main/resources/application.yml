spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/datajpa
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
#        default_batch_fetch_size: 100 # in 쿼리로 가져온다. in쿼리의 개수를 몇개로 할것인가?

logging:
  level:
    org.hibernate.SQL: debug


